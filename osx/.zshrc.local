alias emacs="$(brew --prefix emacs)/Emacs.app/Contents/MacOS/Emacs -nw"

# for nginx to run on port 80
alias sudo="sudo "

# use homebrew
export PATH=/usr/local/bin:$PATH

# brew cask
export HOMEBREW_CASK_OPTS="--appdir=/Applications"

# docker
#eval "$(boot2docker shellinit)"
